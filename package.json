{
  "name": "v3-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/",
    "build:github": "vite build --base=/v3-vite/",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "vue": "^3.5.32"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.35.0",
    "@vitejs/plugin-vue": "^6.0.6",
    "vite": "^8.0.10",
    "wrangler": "^4.87.0"
  }
}
